ASCII text strings (using double single quotes [''] in the TOML)
bitfields (|t| in the TOML)
using a field inside of a struct as the table that struct indexes into (using / in the TOML)
helper fields (|= in the TOML; for example baseStatTotal|=hp+attack+def+speed+spatk+spdef)
graphics and palette editing/insertion
c injection
find and replace (filling unused bytes with FF)
jump to pointer if double-clicking inside a struct
convert structs and functions to toml
ctrl + t pulls up all 3 editors
port firered editor code to hex_editor.py
generate toml (other option)
undo (ctrl + z)
trainer team editing
overworld sprite editing
egg moves